# Ensure consistent line endings across operating systems
* text=auto

# Git hooks and shell scripts must strictly use LF line endings
.githooks/* text eol=lf
scripts/*.sh text eol=lf
*.sh text eol=lf
